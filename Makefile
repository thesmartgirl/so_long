cc -Wall -Werror -Wextra so_long.c manage_window.c render.c move.c map_file.c map_check.c map_utils.c init.c -L./mlx -lmlx -lXext -lX11 -L./libft -lft
Doesnt count moves when collecting
Display total moves when winning the game.
trailing and leading space
out of screen rendering
check if we still need game.images_init
rename maps?.c files